// Tu możesz dodać swoje skrypty JavaScript, interakcje i inne funkcje

// Przykładowa funkcja do obsługi kliknięcia na kartę gry
const gameCards = document.querySelectorAll('.game-card');
gameCards.forEach(card => {
  card.addEventListener('click', () => {
    // Dodaj kod obsługujący kliknięcie na kartę gry
  });
});

// Dodaj więcej skryptów JavaScript tutaj
